
# 예를들어 main_func.py라는 폴더를 만들고

def say_hi():
	print('안녕!')
	
# 이런 코드를 만들었다고 해보자.... 
	# 그런데 이걸 다른 파이썬 파일에서 불러와야 한다면?
	
# from main_func import *

# say_hi()

# 이런식으로불러오면된다.
# 혹은 "from 가져올 디렉터리 혹은 파일명 import 가져올 파일, 함수명 "이런식으로 가져오면 된다.